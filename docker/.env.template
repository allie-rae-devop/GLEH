# ========================================
# GLEH Docker Environment Template
# ========================================
# Copy this file to .env and customize for your deployment
# .env is gitignored to protect sensitive values
#
# For deployment:
#   1. Copy this template: cp .env.template .env
#   2. Edit .env with your values
#   3. Run: export $(cat .env | xargs) && docker-compose up -d
#
# OR use environment variables directly:
#   NGINX_PORT=3080 NGINX_HTTPS_PORT=3443 docker-compose up -d
#
# ========================================

# ========================================
# Network Ports
# ========================================
# Nginx ports (3080/3443 for cloud deployment where NPM uses 80/443)
# Change to 80/443 if running standalone without reverse proxy
NGINX_PORT=3080
NGINX_HTTPS_PORT=3443

# ========================================
# Database Configuration
# ========================================
DB_NAME=gleh_db
DB_USER=gleh_user
DB_PASSWORD=change_me_in_production

# ========================================
# Flask Application
# ========================================
FLASK_ENV=production
FLASK_DEBUG=0
SECRET_KEY=change_me_in_production_use_secrets_generate_key

# ========================================
# Calibre & Calibre-Web
# ========================================
# Calibre-Web internal URL (Docker network name for API calls)
CALIBRE_WEB_URL=http://calibre-web:8083

# Calibre-Web external URL (for browser access to covers and reader)
# Use localhost for local deployments, your domain for production
CALIBRE_WEB_EXTERNAL_URL=http://localhost:8083

# Calibre UIDs/GIDs
CALIBRE_PUID=1024
CALIBRE_PGID=100
CALIBRE_PASSWORD=change_me

# Calibre ports
CALIBRE_PORT=8080
CALIBRE_SERVER_PORT=8081

# Calibre-Web settings
CALIBRE_WEB_PUID=1024
CALIBRE_WEB_PGID=100
CALIBRE_WEB_PORT=8083

# Timezone
TZ=America/Los_Angeles

# ========================================
# Logging
# ========================================
LOG_LEVEL=INFO
LOG_JSON_FORMAT=true
